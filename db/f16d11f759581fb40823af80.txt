(*/ω＼*)<hr /><p>服务器让家里人放别的地方托管了才9天就莫名其妙重启了。有点不爽，在学校丢着的时候可是没人管也能稳定运行一年多的。</p><h2 id="重启原因定位"><a href="#重启原因定位" class="headerlink" title="重启原因定位"></a>重启原因定位</h2><p>还是<code>last</code>和<code>journalctl</code>两个工具。Linux本来可观测性就不错，有了systemd之后定位排查问题更方便了。</p><p>在 Arch Linux（或任何 systemd 系统）中，查看上次关机原因的常用方法如下：</p><p>下面是GPT的输出。</p><hr><h3 id="✅-方法-1：使用-journalctl-查看关机日志"><a href="#✅-方法-1：使用-journalctl-查看关机日志" class="headerlink" title="✅ 方法 1：使用 journalctl 查看关机日志"></a>✅ 方法 1：使用 <code>journalctl</code> 查看关机日志</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">journalctl -b -1 -e</span><br></pre></td></tr></table></figure><ul><li><p><code>-b -1</code> 表示查看上一次启动（即当前之前的引导）。</p></li><li><p><code>-e</code> 表示跳转到日志末尾（关机日志通常在最后）。</p></li></ul><p>你也可以过滤掉关机或重启相关的日志：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">journalctl -b -1 | grep -i <span class="string">&#x27;shutdown\|reboot\|poweroff\|crash&#x27;</span></span><br></pre></td></tr></table></figure><hr><h3 id="✅-方法-2：查看系统是否是非正常关机"><a href="#✅-方法-2：查看系统是否是非正常关机" class="headerlink" title="✅ 方法 2：查看系统是否是非正常关机"></a>✅ 方法 2：查看系统是否是非正常关机</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">last -x</span><br></pre></td></tr></table></figure><p>这会列出关机（shutdown）、重启（reboot）、运行级别变更（runlevel）等信息。</p><ul><li>如果看到类似以下内容，表示可能是非正常断电：</li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">reboot   system boot  5.10.16-arch1-1  Mon Jul  7 12:34   still running</span><br><span class="line">shutdown system down  5.10.16-arch1-1  Mon Jul  7 12:33 - 12:34  (00:01)</span><br></pre></td></tr></table></figure><ul><li>但如果有 <strong><code>crash</code></strong> 或关机后没有 <code>shutdown</code> 条目，则可能是硬件断电或崩溃。</li></ul><hr><h3 id="✅-方法-3：查找是否发生了内核崩溃（Kernel-Panic）"><a href="#✅-方法-3：查找是否发生了内核崩溃（Kernel-Panic）" class="headerlink" title="✅ 方法 3：查找是否发生了内核崩溃（Kernel Panic）"></a>✅ 方法 3：查找是否发生了内核崩溃（Kernel Panic）</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">journalctl -k -b -1</span><br></pre></td></tr></table></figure><p>或者：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dmesg -T | grep -i <span class="string">&#x27;panic\|error\|fail&#x27;</span></span><br></pre></td></tr></table></figure><p>如果你启用了 <code>kdump</code> 或内核转储（coredump）功能，也可以查看是否生成了崩溃日志。</p><hr><h3 id="✅-方法-4：查看-systemd-的退出状态"><a href="#✅-方法-4：查看-systemd-的退出状态" class="headerlink" title="✅ 方法 4：查看 systemd 的退出状态"></a>✅ 方法 4：查看 systemd 的退出状态</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">systemctl status</span><br></pre></td></tr></table></figure><p>或者看 <code>systemd-analyze</code> 提供的引导信息：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">systemd-analyze</span><br></pre></td></tr></table></figure><p>如果你怀疑是硬件或突然断电，也可以查看 <code>dmesg</code> 中是否有电源相关的报错，比如电源丢失、电池低电压等：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dmesg | grep -i <span class="string">&#x27;power\|battery\|acpi&#x27;</span></span><br></pre></td></tr></table></figure><hr><p>ok，journalctl直接看发现到那个时间节点后直接空白，10分钟后才有第二部分日志。systemctl status倒是能看到系统启动时间是15:40就是不知道为啥重启。</p><p>跟着gpt一顿分析，方法二加个<code>less</code>然后滚动日志到开头，发现：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">reboot   system boot  6.15.4-arch2-1   Mon Jul  7 15:40   still running</span><br></pre></td></tr></table></figure><p>疑似是异常断电重启。</p><p>随后可以继续<code>journalctl --since &quot;2025-07-07 15:00&quot; --until &quot;2025-07-07 15:40&quot;</code>来看看这段时间的日志有无蛛丝马迹。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Jul 07 15:30:01 ark-station-server CROND[179952]: (xeonds) CMDEND (/bin/bash /home/xeonds/cf-tunnel/update_cloudflare_dns.sh)  </span><br><span class="line">Jul 07 15:30:01 ark-station-server CROND[179952]: pam_unix(crond:session): session closed for user xeonds  </span><br><span class="line">-- Boot 06bd4039cdda4767be036944ef8668ad --  </span><br><span class="line">Jul 07 15:40:41 ark-station-server kernel: Linux version 6.15.4-arch2-1 (linux@archlinux) (gcc (GCC) 15.1.1 20250425, GNU ld (GNU Binutils) 2.44.0) #1 SMP PREEMPT_DYNAMIC Fri, 27 Jun 20&gt;</span><br></pre></td></tr></table></figure><p>没任何异常。所以基本实锤要么硬件问题，要么人为断电重启。</p><p>明天问问去。不然硬盘&#x2F;数据寄了哭都没地方哭。</p><blockquote><p>得出重拳.jpg</p></blockquote>